@model IEnumerable<WebTotNghiep.Models.HangSX>

@{
    Layout = "~/Areas/Admin/Views/Shared/Layout_admin.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Mota)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Website)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LienHe)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Mota)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Website)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LienHe)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IdHang }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IdHang })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
